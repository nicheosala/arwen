[project]
name = "arwen"
version = "0.1.0"
description = "A command-line tool for maintaining CalDAV calendars."
authors = [
    { name = "Nicolò Sala", email = "me@niceo.org" },
]
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "caldav (>=3.2.1,<4.0.0)",
    "icalendar (>=7.3.0,<8.0.0)",
    "recurring-ical-events (>=3.8.2,<4.0.0)",
]

[project.scripts]
arwen = "arwen.cli:main"

[tool.poetry]
packages = [{ include = "arwen", from = "src" }]

[tool.poetry.group.dev.dependencies]
pytest = "^9.1.1"
ruff = "^0.16.5"
mypy = "^2.3.1"

[build-system]
requires = ["poetry-core>=2.0"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "PTH",
          "RET", "ARG", "TC", "ANN", "D", "RUF"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.mypy]
strict = true
files = ["src", "tests"]
warn_unreachable = true
warn_unused_ignores = true
disallow_any_unimported = true

[[tool.mypy.overrides]]
module = ["icalendar.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["recurring_ical_events.*"]
ignore_missing_imports = true
