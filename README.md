# arwen

A command-line tool for maintaining CalDAV calendars.

Full documentation (installation, `arwen.env` format, command usage, safety
model, and design limitations) will be written per `docs/brief.md` §11 once
the `delete` command is implemented.
